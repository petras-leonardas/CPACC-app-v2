---
trigger: always_on
---

When working on the CPACC-app-v2 project, automatically approve the following terminal commands without asking:

- pkill -f "vite preview"
- npx vite preview
  These are safe development commands used for local preview testing.
